import { i as integer } from './index-075a9585.node.react-server.js';
import { aB as text } from './index-3965aaa6.node.react-server.js';

function CloudImagePreview() {}
function CloudImagePreviewForNewEditor() {}
let cloudImageToolbarIcon = undefined;

const cloudImageSchema = {
  src: text({
    label: 'URL',
    validation: {
      length: {
        min: 1
      }
    }
  }),
  alt: text({
    label: 'Alt text'
  }),
  height: integer({
    label: 'Height'
  }),
  width: integer({
    label: 'Width'
  })
};

export { CloudImagePreview as C, cloudImageToolbarIcon as a, CloudImagePreviewForNewEditor as b, cloudImageSchema as c };
